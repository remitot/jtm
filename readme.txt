Задачи развития




общие-------------------------
собирать на maven
сделать общую сборку всех проектов сразу



jtm-core------------------------

1. Строка "UnfoldedComment" сейчас упоминается в двух классах, а можно енкапсулировать логику в одном классе.
2. В чём смысл проставления атрибута commentIndex нодов UnfoldedComment? Тем более что некоторые UnfoldedComment - ноды (созданные только что, в момент выполнения connection.setActine(false)) не имеют commentIndex.





jtm-web--------------------------

1. Разобраться с маппингом датасурсов: Context.ResourseLink->Server.Resource
  Что если вообще обойтись без Context.ResourseLink (судя по всему, JNDI может искать только по Server.Resource)
  Что если несколько ResourceLink ссылается на один Resource? Если удалить/закомментировать тот ресурс, то одвалятся все ResourceLink?
  Активный/неактивный JDBC: комментирования только ResourceLink недостаточно. Но можно ли комментировать Resource? А если связь будет многие-к-одному? А если данный Resource используют не только ResourceLinkи, но и другие глобальные объекты?
  
2. Сделать клиентскую валидацию полей на jdbc/mod по аналогии с jam-web/jk/mod
  
3. Можно ли создавать JDBC с одним и тем же именем, (скорее всего, нельзя, даже если один из них закомментирован, а второй нет (иначе что делать при раскомментирговании?)). 




jam-web------------------

1. Сделать api-docs.html по аналогии с jtm-web

2. Вынести jam-core по аналогии с jtm-core




web-common-------------------------

1. Разобраться с классами в JS (переопределение функция на прикладном уровне)  